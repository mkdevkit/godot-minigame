{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the packer main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
